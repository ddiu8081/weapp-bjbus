// pages/my/favorite.js
var observer = require('../../libs/observer').observer;

Page(observer({
  props: {
    data: require('../../stores/globalData').default,
  },
  onLoad: function (options) {
  },
  onShow: function () {
  }
}))